package com.maalsaathi.app.data.models

enum class TripStatus { ONGOING, COMPLETED, CANCELLED, SCHEDULED }
enum class EntryType { EXPENSE, INCOME, NOTE }
enum class ReminderType { ADVANCE_LENA, UDHARI_CHUKANI, TRIP_SCHEDULED, CUSTOM }
enum class UserRole { MAALIK, DRIVER, MAALIK_DRIVER }
enum class InviteStatus { PENDING, ACCEPTED, EXPIRED }
